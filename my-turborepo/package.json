{
  "name": "esgriba-lms",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "setup": "pnpm install && cd packages/prisma && pnpm db:generate && pnpm db:migrate && pnpm db:seed",
    "db:setup": "cd packages/prisma && pnpm db:generate && pnpm db:migrate && pnpm db:seed",
    "db:reset": "cd packages/prisma && pnpm db:push && pnpm db:seed"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.6.3",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
